name: cold_storage_logistics_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  google_maps_flutter: ^2.1.1
  google_ml_kit: ^0.19.0  # Updated to the latest version
  provider: ^6.0.0
  supabase_flutter: ^2.0.0  # Added Supabase client library

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
